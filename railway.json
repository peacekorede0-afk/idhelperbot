{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python youtube_fileid_bot.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
